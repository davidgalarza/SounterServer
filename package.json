{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "pm2 start index.js -i max — name='SounterServer'",
    "restart": "pm2 restart all"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "google-translate-api": "^2.3.0",
    "pm2": "^3.0.4",
    "ytdl-core": "^0.25.0"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "nock": "^9.4.4",
    "nodemon": "^1.18.3"
  }
}
